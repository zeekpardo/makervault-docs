---
title: Bulk Tagging
description: Tag multiple files at once with multi-select.
---

When you need to tag many files at once — like an entire bundle you just downloaded — MakerVault's bulk tagging makes it fast.

## Selecting multiple files

| Action | Result |
|---|---|
| **Click** | Select a single file |
| **Cmd + Click** | Add/remove individual files from the selection |
| **Shift + Click** | Select a range of files from the last clicked file |

## The bulk tag bar

When multiple files are selected, a bulk tag bar appears showing "Tag N files". Use the tag input to apply tags to all selected files at once.

## Common workflows

### Tagging a new bundle

1. Add the folder containing the bundle.
2. Filter by that folder in the sidebar.
3. Select all files (Cmd+A or Shift+click from first to last).
4. Use the bulk tag bar to apply tags like the source name, material type, or project category.

### Organizing by category

1. Search for a file type (e.g., type "SVG" in search).
2. Select all matching files.
3. Apply a category tag.
